library(MetropolisHastings)
b <- 0.2
w <- 0.25
n <- 5000
burn <- 1000
days <- 250
win <- tabulate(1:5, size=days, replace=T, prob=c(1, 1-b, 1-2*b,b))
win <- tabulate(sample(1:5, size=days, replace=T, prob=c(1, 1-b, 1-2*b,b)))
win <- tabulate(sample(1:5, size=days, replace=T, prob=c(1, 1-b, 1-2*b, 2*b,b)))
res <- Bayesian.inference(n, w, win)
q()
